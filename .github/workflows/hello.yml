name: hello_world!

on: 
  push: 
    branches:
      - main

jobs: 
  hello_world_job:
    runs-on: ubuntu-latest
    steps: 
      - name: hello 
        run : | 
            echo "hello world!"
      - name: show_date
        run: date
      - name: show_dirs
        run: ls -l

